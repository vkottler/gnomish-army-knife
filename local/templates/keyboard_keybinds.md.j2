# Keyboard Keybinds

([back](../README.md))

Total: `{{addon["bindings"]|length}}`

{% import __templates__["macros"] as m %}
Command | Key | Modifiers
--------|-----|----------
{% for bind in addon["bindings"] %}
{% if "command" in bind and bind["command"] %}
{% if "ACTION" in bind["command"] %}`{{bind["command"]}}`{% else %}{{bind["command"]}}{% endif %} | {{addon["keymap"][bind["key"]]}} | {% if "modifiers" in bind %}{{m.mod_str(addon, bind["modifiers"], false)}}{% endif +%}
{% endif %}
{% endfor %}
